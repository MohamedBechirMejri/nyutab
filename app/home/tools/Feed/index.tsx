import RSS from "./RSS";

const Feed = () => {
	return (
		<div className="w-full h-full max-w-full overflow-hidden bg-black shadow-xl select-none bg-opacity-30 backdrop-blur-3xl rounded-xl">
			<RSS />
		</div>
	);
};

export default Feed;
